export interface IExport {
    _id:string;
  country: string;  
  code: string;  
  flag : string;    
  volume : string; 
  category: string; 
  isActive: boolean;
  createdAt: Date;
  updatedAt: Date;
  countryId:string;
}